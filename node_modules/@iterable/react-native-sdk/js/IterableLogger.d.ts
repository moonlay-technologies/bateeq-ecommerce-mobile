import IterableConfig from "./IterableConfig";
declare class IterableLogger {
    readonly config: IterableConfig;
    constructor(config: IterableConfig);
    log(message: String): void;
}
export { IterableLogger };
