'use strict';
export default class IterableUtil {
    static readBoolean(dict, key) {
        if (dict[key]) {
            return dict[key];
        }
        else {
            return false;
        }
    }
}
