// eslint-disable-next-line @typescript-eslint/no-extraneous-class
export class TestHelper {
    static async delayed(delay, fn) {
        return await new Promise(resolve => setTimeout(() => {
            fn();
            resolve();
        }, delay));
    }
}
